# CSDN / 知乎图片回归调查计划

## 目标

确认 `feature/ui-2.0` 相对 `main` 的图片处理路径改动，是否引入了 CSDN、知乎图片回归；通过官方编辑器与插件端 Chrome DevTools 取证，定位是共用图片链路、平台返回映射，还是新增的 eval/crypto 修复副作用。

## 当前阶段

- 状态：已完成；CSDN 与知乎均经用户 V2 宿主手验通过。
- 基线：`main` 作为未重构对照；当前工作分支是 `feature/ui-2.0`。
- 约束：先证据后改动；必须包含知乎 DevTools 取证页；不要只按单平台猜测。

## 阶段

- [x] 阶段 1：对比 `main` 与 `feature/ui-2.0` 的图片管线相关 diff，列出可能受影响的共用入口。
- [x] 阶段 2：在 CSDN 与知乎官方编辑器中用 DevTools 取证上传、正文插入、最终图片 URL。
- [x] 阶段 3：对照插件发布链路抓包，核对图片发现、上传、替换、回写各环节。
- [x] 阶段 4：判断回归点并整理可证伪假设。
- [x] 阶段 5：基于证据生成修复方案并实施：默认平台图床修复、知乎 OSS SDK 显式加载修复。

## 关键约束

- 回复使用中文；Git 提交说明如需要则使用英文。
- 评判基线必须包含 `main`；当前实现基线为 `feature/ui-2.0`。
- 只做取证与规划，不在证据不足时直接改图床/图片算法。
- 优先验证共用层，再看平台差异。


