/*
 * Copyright (c) 2023, Terwer . All rights reserved.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 *
 * V2 UI 基础样式
 * 所有样式都包裹在 .syp-v2 命名空间下，确保与思源笔记样式隔离
 */

@import "./variables.styl"

.syp-v2
  font-family $syp-font-family
  font-size $syp-font-size-sm
  color $syp-text-primary
  line-height 1.5
  -webkit-font-smoothing antialiased
  -moz-osx-font-smoothing grayscale
  min-width 0
  height calc(100vh - 180px)
  max-height calc(100vh - 180px)

  *, *::before, *::after
    box-sizing border-box

  .syp-btn
    display inline-flex
    align-items center
    justify-content center
    padding $syp-spacing-sm $syp-spacing-md
    border none
    border-radius $syp-radius-md
    font-size $syp-font-size-sm
    font-weight 500
    cursor pointer
    transition all 0.2s ease
    outline none

    &:hover
      opacity 0.9

    &:active
      opacity 0.8

    &.syp-btn-primary
      background-color $syp-primary
      color white

    &.syp-btn-secondary
      background-color $syp-bg-secondary
      color $syp-text-primary

    &.syp-btn-text
      background-color transparent
      color $syp-text-secondary

      &:hover
        background-color $syp-bg-secondary

  .syp-card
    background-color $syp-bg-primary
    border-radius $syp-radius-lg
    box-shadow $syp-shadow-sm
    border 1px solid $syp-border-secondary
    padding $syp-spacing-md
    transition box-shadow 0.2s ease

    &:hover
      box-shadow $syp-shadow-md

  .syp-input
    width 100%
    padding $syp-spacing-sm $syp-spacing-md
    border 1px solid $syp-border-primary
    border-radius $syp-radius-md
    font-size $syp-font-size-sm
    color $syp-text-primary
    background-color $syp-bg-primary
    transition border-color 0.2s ease

    &:focus
      outline none
      border-color $syp-primary

    &::placeholder
      color $syp-text-tertiary

  .syp-toggle
    position relative
    display inline-block
    width 44px
    height 24px

    input
      opacity 0
      width 0
      height 0

      &:checked + .syp-toggle-slider
        background-color $syp-primary

      &:checked + .syp-toggle-slider:before
        transform translateX(20px)

    .syp-toggle-slider
      position absolute
      cursor pointer
      top 0
      left 0
      right 0
      bottom 0
      background-color $syp-border-primary
      border-radius 24px
      transition 0.3s

      &:before
        position absolute
        content ""
        height 18px
        width 18px
        left 3px
        bottom 3px
        background-color white
        border-radius 50%
        transition 0.3s

  .syp-panel
    display flex
    flex-direction column
    height 100%
    background linear-gradient(180deg, rgba(255, 255, 255, 0.98) 0%, rgba(248, 250, 253, 0.98) 100%)
    border-radius $syp-radius-xl
    box-shadow $syp-shadow-lg
    border 1px solid rgba(222, 224, 227, 0.9)
    overflow hidden

  .syp-header
    display flex
    align-items center
    justify-content space-between
    padding $syp-spacing-md $syp-spacing-lg
    border-bottom 1px solid $syp-border-secondary
    flex-shrink 0

    .syp-header-title
      font-size $syp-font-size-lg
      font-weight 600
      color $syp-text-primary

  .syp-content
    padding $syp-spacing-lg
    overflow-y auto

  .syp-list
    list-style none
    margin 0
    padding 0

    .syp-list-item
      display flex
      align-items center
      padding $syp-spacing-md
      border-bottom 1px solid $syp-border-secondary
      cursor pointer
      transition background-color 0.2s ease

      &:hover
        background-color $syp-bg-secondary

      &:last-child
        border-bottom none

  .syp-empty
    display flex
    flex-direction column
    align-items center
    justify-content center
    padding $syp-spacing-xl
    color $syp-text-tertiary
    text-align center

    .syp-empty-icon
      font-size 48px
      margin-bottom $syp-spacing-md

    .syp-empty-text
      font-size $syp-font-size-md
      margin-bottom $syp-spacing-sm

    .syp-empty-subtext
      font-size $syp-font-size-sm

  .syp-shell
    display grid
    gap 0
    min-height 0
    flex 1

    &.is-quick-publish
      grid-template-columns minmax(0, 1fr)

    &.is-settings
      grid-template-columns 196px minmax(0, 1fr)

  .syp-shell__nav
    padding $syp-spacing-lg
    background linear-gradient(180deg, #f7f9fc 0%, #eef2f6 100%)
    border-right 1px solid $syp-border-secondary
    overflow-y auto
    min-height 0

  .syp-shell__section-title
    margin-bottom $syp-spacing-md
    font-size $syp-font-size-xs
    color $syp-text-tertiary
    letter-spacing 0.08em
    text-transform uppercase

  .syp-shell__nav-item
    width 100%
    text-align left
    border none
    background transparent
    color $syp-text-secondary
    padding 12px 14px
    border-radius $syp-radius-lg
    margin-bottom $syp-spacing-sm
    cursor pointer
    transition all 0.2s ease

    &:hover
      background rgba(64, 128, 255, 0.08)
      color $syp-text-primary

    &.is-active
      background white
      color $syp-text-primary
      box-shadow $syp-shadow-sm

  .syp-shell__main
    display flex
    flex-direction column
    padding $syp-spacing-xl
    background $syp-bg-primary
    min-width 0
    min-height 0
    overflow hidden

  @media (max-width: 960px)
    .syp-v2
      height auto
      max-height calc(100vh - 96px)

    .syp-shell
      &.is-quick-publish
        grid-template-columns 1fr

      &.is-settings
        grid-template-columns 1fr

    .syp-shell__nav
      border none
